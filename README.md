# Brazil Forest Model 🌲

A CHAP-compatible Random Forest regression model predicting dengue cases based on rainfall and mean temperature.

## Train
```bash
python train.py example_data/brazil.csv output/model.bin
